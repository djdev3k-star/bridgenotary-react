version: '3.8'

services:
  backend:
    build: .
    container_name: bridgenotary-backend
    ports:
      - "5000:5000"
    environment:
      # Server Configuration
      PORT: 5000
      NODE_ENV: production
      
      # Odoo Configuration (set via environment variables)
      ODOO_URL: ${ODOO_URL}
      ODOO_DB: ${ODOO_DB}
      ODOO_USERNAME: ${ODOO_USERNAME}
      ODOO_API_KEY: ${ODOO_API_KEY}
      ODOO_FORM_TOKEN: ${ODOO_FORM_TOKEN}
      
      # Proof API Configuration
      PROOF_API_KEY: ${PROOF_API_KEY}
      PROOF_ENVIRONMENT: test
    
    # Connect to Tailscale network (if Tailscale is running)
    networks:
      - bridge_network
    
    # Restart policy
    restart: unless-stopped
    
    # Logs
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # Optional: Nginx service for containerized deployment
  # Uncomment to use Docker for Nginx as well
  # nginx:
  #   image: nginx:latest
  #   container_name: bridgenotary-nginx
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
  #     - /var/www/bridgenotary:/usr/share/nginx/html:ro
  #     - /etc/letsencrypt:/etc/letsencrypt:ro
  #   depends_on:
  #     - backend
  #   networks:
  #     - bridge_network
  #   restart: unless-stopped
  #   logging:
  #     driver: "json-file"
  #     options:
  #       max-size: "10m"
  #       max-file: "3"

networks:
  bridge_network:
    driver: bridge
