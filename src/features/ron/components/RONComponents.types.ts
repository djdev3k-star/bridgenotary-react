export interface RONFeatureCardProps {
  icon: React.ReactNode;
  title: string;
  desc: string;
}

export interface RONStatProps {
  number: string;
  label: string;
}

export interface RONIndustryCardProps {
  title: string;
  desc: string;
}
