export interface RONFeatureCardProps {
  icon: React.ReactNode;
  title: string;
  desc: string;
}

export interface ApostilleScenarioProps {
  situation: string;
  documents: string;
  gotcha: string;
  icon: React.ReactNode;
}

export interface ApostilleSolutionProps {
  action: string;
  benefit: string;
  icon: React.ReactNode;
}
